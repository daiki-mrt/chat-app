<div class="side-header">
  <div class="side-header-left">
    <%= link_to current_user.name, edit_user_path(current_user), class: "current-user-name" %>
  </div>
  <div class="side-header-left">
    <%= link_to "チャットを作成する", new_room_path, class: "create-chat-btn" %>
  </div> 
</div>
<div class="rooms-area">
  <% current_user.rooms.each do |room| %>
    <div class="rooms">
      <a href="#" class="room"><%= room.name %></a>
    </div>
  <% end %>
</div>  